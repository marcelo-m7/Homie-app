# Homie Flask Application - Environment Variables Sample
# Copy this file to .env and update the values for your environment

# ===== Flask Configuration =====
# Secret key for session management and CSRF protection
# Generate a secure random key: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-here-change-in-production

# Flask debug mode (set to False in production)
FLASK_DEBUG=False

# Application base URL (used for OIDC redirects and security validation)
BASE_URL=http://localhost:5000

# Application port (default: 5000)
PORT=5000

# ===== Database Configuration =====
# Optional: Override default database path
# DATABASE_PATH=./data/homie.db

# ===== OIDC Authentication Configuration =====
# Enable OIDC authentication (set to false to disable OIDC and prepare for local accounts)
OIDC_ENABLED=true

# OIDC provider base URL (required when OIDC is enabled)
OIDC_BASE_URL=https://your-oidc-provider.com

# OIDC client credentials (get these from your OIDC provider)
OIDC_CLIENT_ID=your-client-id
OIDC_CLIENT_SECRET=your-client-secret

# Optional: Manual OIDC endpoint configuration (if auto-discovery fails)
# OIDC_ISSUER=https://your-oidc-provider.com
# OIDC_AUTHORIZATION_ENDPOINT=https://your-oidc-provider.com/auth/realms/your-realm/protocol/openid-connect/auth
# OIDC_TOKEN_ENDPOINT=https://your-oidc-provider.com/auth/realms/your-realm/protocol/openid-connect/token
# OIDC_USERINFO_ENDPOINT=https://your-oidc-provider.com/auth/realms/your-realm/protocol/openid-connect/userinfo
# OIDC_JWKS_URI=https://your-oidc-provider.com/auth/realms/your-realm/protocol/openid-connect/certs
# OIDC_END_SESSION_ENDPOINT=https://your-oidc-provider.com/auth/realms/your-realm/protocol/openid-connect/logout

# ===== Access Control Configuration =====
# Allowed email addresses (comma-separated list of who can access the app)
# ALLOWED_EMAILS=dev@example.com,family@example.com

# ===== Security Configuration =====
# Enable secure cookies (set to True in production with HTTPS)
SESSION_COOKIE_SECURE=False

# ===== Logging Configuration =====
# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# ===== Example Values for Development =====
# Uncomment and modify these for local development:

# SECRET_KEY=dev-secret-key-change-me
# FLASK_DEBUG=True
# BASE_URL=http://localhost:5000
# PORT=5000
# OIDC_ENABLED=true
# OIDC_BASE_URL=https://auth.example.com
# OIDC_CLIENT_ID=homie-app
# OIDC_CLIENT_SECRET=your-dev-client-secret
# ALLOWED_EMAILS=dev@example.com,family@example.com
# LOG_LEVEL=DEBUG

# ===== Production Notes =====
# In production, ensure:
# 1. SECRET_KEY is a secure random string
# 2. FLASK_DEBUG=False
# 3. SESSION_COOKIE_SECURE=True (with HTTPS)
# 4. BASE_URL uses HTTPS
# 5. All OIDC URLs use HTTPS (if OIDC_ENABLED=true)
# 6. LOG_LEVEL=INFO or WARNING
